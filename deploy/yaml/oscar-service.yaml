apiVersion: v1
kind: Service
metadata:
  name: oscar
  namespace: oscar
spec:
  type: NodePort
  ports:
    - name: endpoint
      port: 8080
      protocol: TCP
      targetPort: 8080
      nodePort: 8080
  selector:
    app: oscar